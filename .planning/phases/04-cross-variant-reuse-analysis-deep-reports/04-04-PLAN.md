---
phase: 04-cross-variant-reuse-analysis-deep-reports
plan: 04
type: execute
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - app/services/html_pages.py
  - app/services/report.py
  - app/backend/cli.py
  - tests/test_pages.py
requirements:
  - ANALYSIS-02
autonomous: true
---

<objective>
Render the comparison as `compare.html` and show the data-quality trail on the existing report.

Purpose: The counts from plans 04-01 through 04-03 are the demo. This plan puts them on the static site the other pages already use, with Standard vs Nordic open first.
Output: `data/processed/compare.html` from the same command that rewrites the site, `compare.json` from `analyze compare`, and file/row text on the report's data-issues section.
</objective>

<execution_context>
./.cursor/get-shit-done/workflows/execute-plan.md
./.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cross-variant-reuse-analysis-deep-reports/04-CONTEXT.md
@.planning/phases/04-cross-variant-reuse-analysis-deep-reports/04-01-SUMMARY.md
@.planning/phases/04-cross-variant-reuse-analysis-deep-reports/04-02-SUMMARY.md
@.planning/phases/04-cross-variant-reuse-analysis-deep-reports/04-03-SUMMARY.md
@app/services/html_pages.py
@app/services/report.py
@app/backend/cli.py
@app/services/analysis.py
@app/services/quality.py

**Discovery:** Level 0. No new libraries. No web framework.

**Constraining decisions:**
- Same shell and nav as `workflow.html`. Add `compare.html` with label `4 · Compare`. Do not restyle the other pages.
- All pairs from `all_variant_pairs()` are in the file. One section is visible. A few lines of script in the page show the selected section and hide the others. There is no server and no fetch.
- The visible section on load is `REGIO-STD` on the left and `REGIO-NORDIC` on the right when both variants exist. Other sections keep lexicographic left/right.
- A component or blocker link is an anchor to an element already in the file. Show `source_file` and `source_row`. For a blocked row, show both values.
- Rows with `high_overlap` true get a CSS class `high-overlap` that is visually distinct (background or border). Counts sit next to the ratio.
- Data-issues on `report.html` print each issue's `records` file and row. Keep the five existing report headings.
- Do not read `data/ground_truth/`. Do not add Flask, FastAPI, or an API route.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write compare.html</name>
  <files>app/services/html_pages.py, tests/test_pages.py</files>
  <action>
    Add `render_compare(conn)` and include `compare.html` in `PAGES` and `write_site`. Call `build_canonical_model` only if `write_site` / `report` already prepares the database before rendering; do not ingest inside `render_compare`. Call `compare_variants` for each pair from `all_variant_pairs`. For the STD/NORDIC section, call `compare_variants(conn, "REGIO-STD", "REGIO-NORDIC")` so Standard is the left column.

    Each assembly is a row: reference, overlap ratio, shared, left-only, right-only, unresolved, conflicts, candidates, and a `high-overlap` class when `high_overlap` is true. Under it, list components with their label and an anchor id equal to `anchor`. Each component's records show file and row. Give the compare page a wider content width than 820px without changing the shell default for the other pages (a body class or a wrapper on this page only).

    The pair control is a `select` or a row of links. The script only toggles a `hidden` class. Escaping stays `html.escape`. Tests in `tests/test_pages.py` build a two-variant fixture, call `render_compare`, and assert: both variant refs appear, the assembly row shows the ratio, a `high-overlap` class is present for a ratio of at least 0.5, the unresolved or blocked fixture text includes a file name and a row number, and only one pair section lacks the hidden class when both REGIO-STD and REGIO-NORDIC exist. A fixture that has neither of those refs shows the first lexicographic pair instead.
  </action>
  <verify>`python3 -m pytest tests/test_pages.py tests/test_compare.py -q`</verify>
  <done>compare.html is in the nav, opens on Standard vs Nordic when those variants exist, and a high-overlap assembly is marked in the HTML</done>
</task>

<task type="auto">
  <name>Task 2: Trail on the report and compare JSON</name>
  <files>app/services/report.py, app/backend/cli.py, tests/test_pages.py</files>
  <action>
    In the data-issues section of `report.py`, after the existing explanation, print each record as `source_file` and `source_row`. Keep mapping-noise warnings as a summary, not one card per warning. Do not drop the five headings (Already reused, Worth a look, Intentional differences, Blocked, Data issues).

    Extend `analyze` choices with `compare`. It writes `data/processed/compare.json` containing `{"default_pair": ["REGIO-STD", "REGIO-NORDIC"], "pairs": [...]}` where each pair is the `compare_variants` dict. If either default variant is missing, `default_pair` is the first `all_variant_pairs` entry, or an empty list when no variants exist. Call `build_canonical_model` the same way `analyze reuse` already does. Do not point `--db` at a hardcoded absolute inputs path.

    Assert in a test that a rendered data-issue contains the fixture file name and source row, and that the compare JSON written to a tmp path has `default_pair` and at least the assembly ref from the fixture. Do not open `data/ground_truth/`.
  </action>
  <verify>`python3 -m pytest tests/test_pages.py tests/test_quality.py tests/test_compare.py -q`</verify>
  <done>The report names the file and row for a data-quality issue, and `analyze compare` writes compare.json with the default pair</done>
</task>

</tasks>

<verification>
`python3 -m pytest tests/ -q`
</verification>

<success_criteria>
- `compare.html` is produced with the other static pages
- Standard vs Nordic is the open pair when both variants exist
- High overlap is visibly marked, and a component anchor shows file and row
- Blocked rows show both values
- Data issues on `report.html` include the record trail
- `analyze compare` writes JSON under `data/processed/`
- The full test suite passes
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-variant-reuse-analysis-deep-reports/04-04-SUMMARY.md`.
</output>
